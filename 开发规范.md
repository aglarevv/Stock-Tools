# 🧰 StockToolbox 开发结构规划与代码质量规范

> 版本: 1.0 · 最后更新: 2026-06-26 · 适用版本: v2.3.0+

---

## 目录

1. [项目架构总览](#1-项目架构总览)
2. [目录结构规范](#2-目录结构规范)
3. [后端开发规范](#3-后端开发规范)
4. [前端开发规范](#4-前端开发规范)
5. [命名规范](#5-命名规范)
6. [代码质量要求](#6-代码质量要求)
7. [测试规范](#7-测试规范)
8. [Git 工作流](#8-git-工作流)
9. [构建与部署](#9-构建与部署)
10. [文档规范](#10-文档规范)

---

## 1. 项目架构总览

```
┌─────────────────────────────────────────────────┐
│                   桌面壳层                        │
│  macOS: Swift + WKWebView                       │
│  Windows: Electron 31 + NSIS                    │
├─────────────────────────────────────────────────┤
│               React 18 前端 (SPA)                │
│  Vite 5 构建 · CSS 变量主题 · Context 依赖注入    │
│  Pages: 12 个页面 · Hooks: 4 个 · Components: 20+│
├─────────────────────────────────────────────────┤
│          Node.js HTTP Server (端口 8765)          │
│  REST API · RSS 采集 · AI 代理 · StockSDK 封装    │
│  DB 抽象层: MySQL 优先 / SQLite 回退              │
│  Services: stockSdk / rss / classifier / akshare  │
├─────────────────────────────────────────────────┤
│               Stock SDK (npm)                    │
│  A股/港股/美股/基金 行情 · 板块数据 · 资金流向      │
│  北向资金 · 涨停板 · 期货 · 分时走势               │
└─────────────────────────────────────────────────┘
```

**核心设计原则**（沿袭自 README）：

| 原则 | 说明 |
|------|------|
| **低耦合高内聚** | 组件通过 Context 注入依赖，避免直接 import 耦合 |
| **单一职责** | 协调器组件 + 纯展示组件 + 数据层分离 |
| **可测试性** | useApi 通过 Context 注入，可在测试中替换 mock |
| **容错性** | 单模块失败不阻塞其他功能（safeCall 包装） |
| **跨平台一致性** | CSS 变量驱动主题，避免硬编码；WKWebView/Electron 双路径适配 |

---

## 2. 目录结构规范

### 2.1 当前结构（v2.3.0）

```
tools/
├── .github/workflows/          # CI/CD (ci / release / deploy-website)
├── react-app/                  # React 前端应用
│   ├── index.html              # Vite 入口
│   ├── vite.config.js          # Vite 配置
│   ├── package.json            # 前端依赖 + Electron-builder
│   ├── public/icon.png         # 应用图标
│   ├── electron/               # Electron 主进程 + preload
│   ├── macos/                  # macOS 原生壳 (Swift)
│   └── src/
│       ├── main.jsx            # React 挂载入口
│       ├── App.jsx             # 根组件（路由 + Context）
│       ├── styles/             # 全局样式 + CSS 变量 + 字体
│       ├── utils/              # API 客户端 / 工具函数 / 配置
│       ├── hooks/              # 自定义 Hooks (useApi / useToast / useNavigation)
│       └── components/         # 页面组件 + 原子组件
├── src/server/                 # Node.js 后端
│   ├── server.js               # 主入口（HTTP Server + REST 路由）
│   ├── server.bundle.js        # esbuild 构建产物（不提交？已提交）
│   ├── db.js                   # 数据库抽象层
│   ├── keywords.js             # 关键词配置
│   └── services/
│       ├── stockSdk.js         # StockSDK 服务封装 (~977 行)
│       ├── rss.js              # RSS 采集 + OPML 解析
│       ├── classifier.js       # 文章分类 + 情感分析 + 摘要清洗
│       ├── akshare.js          # AKShare Python 桥接
│       └── akshare_bridge.py   # Python 桥接脚本
├── scripts/                    # 构建/混淆脚本
├── website/                    # GitHub Pages 官网
├── sources.opml                # 默认 RSS 源
├── package.json                # 后端依赖
└── README.md                   # 项目文档
```

### 2.2 待改进项

| 问题 | 当前状态 | 建议 |
|------|---------|------|
| `server.bundle.js` 提交到仓库 | 构建产物混入源码 | 添加到 `.gitignore` |
| `server.js` 超 2700 行 | 单文件过重 | 按功能拆分为独立路由模块 |
| `stockSdk.js` 超 970 行 | 单一服务层过大 | 按数据域拆分（行情/板块/资金/北向） |
| 前端 CSS 集中在单文件 | `global.css` 可能过大 | 考虑 CSS Modules 或按组件拆分 |
| 缺少 `.gitignore` 规则 | 构建产物/缓存可能混入 | 补充完整忽略规则 |

### 2.3 推荐演进结构（v2.2+）

```
src/server/
├── server.js                  # 入口：启动服务器 + 全局中间件 (<200行)
├── routes/                    # 路由模块（从 server.js 拆分）
│   ├── trade.js               # 交易计划 / 复盘
│   ├── ai.js                  # AI 对话 / 文件解析 / 摘要
│   ├── stock.js               # StockSDK 行情 / 板块
│   ├── news.js                # 每日时事 / RSS
│   └── settings.js            # 应用设置
├── services/
│   ├── stock/                 # StockSDK 拆分
│   │   ├── index.js           # 统一导出
│   │   ├── quotes.js          # 行情（A/港/美/基金）
│   │   ├── sectors.js         # 行业/概念板块
│   │   ├── fundFlow.js        # 资金流向
│   │   └── northbound.js      # 北向资金
│   ├── rss/                   # RSS 服务
│   └── classifier/            # 分类器
├── middleware/                 # 中间件（CORS, 错误处理, 日志）
├── db/                        # 数据库层
│   ├── index.js               # 统一入口
│   ├── mysql.js               # MySQL 驱动
│   └── sqlite.js              # SQLite 驱动
└── utils/                     # 工具函数（加密 / 格式化 / 验证）

react-app/src/
├── components/
│   ├── ui/                    # 原子 UI 组件（Icon, Button, Modal, Toast）
│   ├── layout/                # 布局组件（Sidebar, TitleBar）
│   ├── pages/                 # 页面组件（Dashboard, SectorBoard, ...）
│   └── features/              # 功能复合组件（DailyReview + 子组件）
├── hooks/                     # 保持不变
├── utils/                     # 保持不变
├── styles/
│   ├── tokens.css             # CSS 变量定义
│   ├── components/            # 组件级样式（可选 CSS Modules）
│   └── global.css             # 全局重置 + 基础排版
└── constants/                 # 常量定义（页面路由 / 配置项）
```

---

## 3. 后端开发规范

### 3.1 模块规范

- **文件头注释**：每个模块必须包含 `@file` 说明，描述职责、设计原则、用法示例
- **严格模式**：所有 `.js` 文件以 `"use strict";` 开头
- **依赖导入**：Node.js 内置模块使用 `node:` 前缀（如 `require("node:fs")`）

```js
/**
 * 模块名称 — 一句话描述
 *
 * 职责：
 * - 职责 1
 * - 职责 2
 *
 * 设计原则：
 * - 原则 1
 * - 原则 2
 *
 * 用法：
 *   const { fn } = require("./module");
 *   await fn();
 */
"use strict";
```

### 3.2 API 路由规范

| 规范 | 说明 |
|------|------|
| URL 前缀 | 所有 API 路径统一以 `/api/` 开头 |
| RESTful 风格 | GET 查询, POST 创建, DELETE 删除 |
| 参数传递 | GET 用 query string, POST 用 JSON body |
| 响应格式 | 统一 `{ data/error }` JSON，设置正确 Content-Type |
| 错误处理 | try-catch 包裹所有异步处理，返回友好错误信息 |
| CORS | 开发环境允许 localhost:5173 |

**新增 API 的标准流程**：

1. 在对应 `services/` 模块中实现业务逻辑
2. 在 `server.js` `handleApi()` 中添加路由
3. 在 `react-app/src/utils/api.js` 中添加客户端方法
4. 编写路由对应的测试用例

### 3.3 数据库规范

- **双引擎兼容**：所有 SQL 语句必须兼容 MySQL 和 SQLite
- **表创建**：使用 `CREATE TABLE IF NOT EXISTS`，建在 `initializeDatabase()` 中
- **列新增**：使用 `db.addColumnIfNotExists()` 热迁移
- **查询**：使用参数化查询，防止 SQL 注入
- **连接池**：MySQL 使用连接池，配置合理的超时和重试

### 3.4 错误处理标准

```js
// ✅ 好的错误处理
async function getQuotes(codes) {
  try {
    const data = await sdk.getFullQuotes(codes);
    return { success: true, data };
  } catch (err) {
    console.error("[stockSdk] getQuotes 失败:", err.message);
    return { success: false, error: "行情数据获取失败", data: [] };
  }
}

// ❌ 差的错误处理
async function getQuotes(codes) {
  const data = await sdk.getFullQuotes(codes);  // 不捕获异常
  return data;
}
```

**`safeCall` 包装器**：所有 SDK 调用必须通过 `safeCall` 包装，确保单功能失败不影响整个 API 响应。

### 3.5 安全规范

| 项目 | 要求 |
|------|------|
| API Key 存储 | AES-256-GCM 加密，不存明文 |
| 密码处理 | 使用 pbkdf2Sync 派生密钥，加盐 |
| HTTP Agent | AI 请求使用独立 Agent，开发环境允许自签名 |
| 敏感日志 | 禁止在日志中打印 API Key / Token |
| 依赖安全 | 定期 `npm audit`，及时升级有漏洞的依赖 |

---

## 4. 前端开发规范

### 4.1 组件规范

| 原则 | 说明 |
|------|------|
| **文件命名** | PascalCase 组件名（`DailyReview.jsx`） |
| **导出方式** | 默认导出页面组件，命名导出工具函数 |
| **Props** | 使用解构接收，避免 `props.xxx` |
| **状态** | useState 就近声明，避免状态提升过度 |
| **副作用** | useEffect 必须指定依赖数组，禁止空依赖滥用 |
| **Context** | useApi / useNavigation 注入依赖，组件不直接 import api |

**组件分类**：

| 类型 | 位置 | 特征 | 示例 |
|------|------|------|------|
| **原子 UI** | `components/ui/` | 无业务逻辑，纯展示 | `Icon`, `Button` |
| **布局** | `components/layout/` | 页面布局结构 | `Sidebar`, `TitleBar` |
| **页面** | `components/pages/` | 对应一个导航项 | `Dashboard`, `SectorBoard` |
| **功能复合** | `components/features/` | 业务逻辑 + 子组件协调 | `DailyReview` + `ReviewSectionCard` |

**组件结构模板**：

```jsx
import { useState, useEffect } from "react";
import { useApi } from "../hooks/useApi.jsx";
import Icon from "./Icon.jsx";

/**
 * 组件名称 — 一句话描述
 * @param {{ navigate: Function, showToast: Function }} props
 */
export default function MyComponent({ navigate, showToast }) {
  const api = useApi();
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    loadData();
  }, []);

  async function loadData() {
    try {
      setLoading(true);
      const result = await api.myEndpoint();
      setData(result);
    } catch (err) {
      showToast("加载失败: " + err.message, "error");
    } finally {
      setLoading(false);
    }
  }

  if (loading) return <div className="page"><div className="empty-text">加载中…</div></div>;
  if (!data) return <div className="page"><div className="empty-text">暂无数据</div></div>;

  return (
    <div className="page">
      {/* 页面内容 */}
    </div>
  );
}
```

### 4.2 样式规范

| 规范 | 说明 |
|------|------|
| **CSS 变量** | 所有颜色、间距、圆角使用 `var(--xxx)` |
| **主题适配** | 通过 `data-theme="dark"` 切换，不在组件中硬编码颜色 |
| **类名命名** | BEM 风格：`.block__element--modifier` |
| **响应式** | 使用 `clamp()` / `min()` 函数，避免固定 px 值 |
| **字体** | Inter（正文）+ JetBrains Mono（等宽/数据），自托管 WOFF2 |

**主题变量使用示例**：

```css
/* ✅ 正确：使用 CSS 变量 */
.card {
  background: var(--bg-card);
  color: var(--text-primary);
  border: 1px solid var(--border);
}

/* ❌ 错误：硬编码色值 */
.card {
  background: #ffffff;
  color: #333333;
  border: 1px solid #e5e5e5;
}
```

### 4.3 新增页面标准流程

1. `react-app/src/components/YourPage.jsx` — 页面组件
2. `react-app/src/App.jsx` — 在 `PAGES` 对象中注册
3. `react-app/src/components/Sidebar.jsx` — 在 `NAV` 数组中添加导航项

### 4.4 性能规范

| 规范 | 说明 |
|------|------|
| **内存缓存** | 板块/行业等低频数据使用 `sessionCache` 缓存（30 分钟 TTL） |
| **惰性加载** | 非激活 Tab 不发送网络请求 |
| **大列表** | 超过 100 条记录实现分页加载 |
| **图片** | 使用 SVG 内联图标，避免额外 HTTP 请求 |
| **bundle 优化** | Vite 自动 tree-shaking，避免手动引入大型库 |

---

## 5. 命名规范

### 5.1 JavaScript / JSX

| 类型 | 规则 | 示例 |
|------|------|------|
| 文件名 | kebab-case 或 PascalCase（组件） | `daily-review.js`, `DailyReview.jsx` |
| 变量/函数 | camelCase | `loadQuotes`, `stockData` |
| 常量 | UPPER_SNAKE_CASE | `API_BASE`, `CACHE_TTL` |
| React 组件 | PascalCase | `DailyReview`, `SectorBoard` |
| 自定义 Hook | `use` 前缀 + camelCase | `useApi`, `useNavigation` |
| 私有函数 | 无下划线，通过 JSDoc `@private` 标记 | `function cacheGet()` |
| 事件处理 | `handle` + 名词 + 动词 | `handleStockSearch`, `handleDelete` |
| 布尔变量 | `is` / `has` / `can` + 形容词 | `isLoading`, `hasError` |

### 5.2 CSS 类名

```css
/* Block */
.sector-card { }

/* Element */
.sector-card__title { }
.sector-card__value { }

/* Modifier */
.sector-card--active { }
.sector-card--warning { }
```

### 5.3 数据库

| 类型 | 规则 | 示例 |
|------|------|------|
| 表名 | snake_case 复数 | `trade_plans`, `daily_reviews` |
| 列名 | snake_case | `buy_price`, `profit_rate` |
| 主键 | `id` 自增 | `id INTEGER PRIMARY KEY AUTOINCREMENT` |
| 外键 | `{table}_id` | `trade_plan_id` |
| 索引 | `idx_{table}_{column}` | `idx_daily_reviews_symbol` |

### 5.4 API 路径

| 类型 | 规则 | 示例 |
|------|------|------|
| 资源集合 | `/api/{noun}` | `/api/trade-plans` |
| 单资源 | `/api/{noun}?id=` | `/api/daily-reviews?id=1` |
| 子资源 | `/api/{noun}/{id}/{sub}` | `/api/stock/industry/BK0477/stocks` |
| 动作 | `/api/{noun}/{verb}` | `/api/ai/chat`, `/api/stock/search` |

**URL 风格**：使用 kebab-case（`trade-plans` 而非 `tradePlans` 或 `trade_plans`）。

---

## 6. 代码质量要求

### 6.1 必须遵守

| # | 规则 | 严重性 |
|---|------|--------|
| 1 | 所有异步调用必须处理错误（try-catch 或 `.catch()`） | 🔴 阻断 |
| 2 | 禁止在日志中打印 API Key / Token / 密码 | 🔴 阻断 |
| 3 | 禁止使用 `eval()` / `new Function()` | 🔴 阻断 |
| 4 | useEffect 必须提供依赖数组 | 🔴 阻断 |
| 5 | 数据库查询必须使用参数化，禁止字符串拼接 SQL | 🔴 阻断 |
| 6 | 用户输入在渲染前必须转义（`escapeHtml`） | 🔴 阻断 |
| 7 | 构建产物（`dist/`, `build/`, `*.bundle.js`）不提交到仓库 | 🔴 阻断 |
| 8 | `package.json` 中依赖必须锁定版本（`^` 允许次版本更新） | 🟡 警告 |

### 6.2 函数要求

| 度量 | 阈值 | 说明 |
|------|------|------|
| 函数行数 | ≤ 60 行 | 超过须拆分 |
| 参数个数 | ≤ 4 个 | 超过用对象参数 |
| 嵌套深度 | ≤ 4 层 | 超过用 early return 重构 |
| 圈复杂度 | ≤ 10 | 避免过多条件分支 |

### 6.3 注释规范

```js
/**
 * 函数功能描述
 *
 * @param {string} symbol - 股票代码
 * @param {"daily"|"weekly"|"monthly"} [period="daily"] - K线周期
 * @param {string} [startDate] - 开始日期 (YYYY-MM-DD)
 * @returns {Promise<{success: boolean, data: object[], error?: string}>}
 */
async function getStockKline(symbol, period = "daily", startDate) { ... }
```

### 6.4 代码审查检查清单

- [ ] 是否符合单一职责原则
- [ ] 错误是否被正确处理
- [ ] SQL 是否双引擎兼容
- [ ] CSS 是否使用变量而非硬编码
- [ ] 新增 API 是否在 `api.js` 中添加了客户端方法
- [ ] 是否有对应的 E2E 测试
- [ ] 敏感数据是否加密存储
- [ ] 构建是否通过（`npm run build`）

---

## 7. 测试规范

### 7.1 测试层次

| 层级 | 工具 | 覆盖范围 | 频率 |
|------|------|---------|------|
| E2E | Playwright (Python) | 完整用户流程 | 每次发版前 |
| 手动 | 浏览器 dev 模式 | UI 交互验证 | 开发时 |

### 7.2 E2E 测试规范

- 测试文件位于项目根目录，命名 `test_{feature}.py`
- 使用 Playwright，headless Chromium
- 每个测试覆盖完整用户路径：导航 → 交互 → 验证 → 清理
- 测试分类：
  - ✅ 通过（功能正常）
  - ⚠️ 警告（非交易时段无数据等预期情况）
  - ❌ 失败（异常行为需修复）
- 报告格式：最终打印通过/警告/失败数量 + JS 控制台错误

**测试模板**：

```python
"""功能描述"""
from playwright.sync_api import sync_playwright

BASE = "http://localhost:5173"

def run():
    with sync_playwright() as p:
        browser = p.chromium.launch(headless=True)
        page = browser.new_page(viewport={"width": 1440, "height": 900})
        errors = []
        page.on("pageerror", lambda err: errors.append(str(err)))

        # 1. 打开应用
        page.goto(BASE)
        page.wait_for_load_state("networkidle")

        # 2. 执行测试步骤
        # ...

        # N. 报告结果
        browser.close()
        return results
```

### 7.3 测试要求

| 要求 | 说明 |
|------|------|
| 新功能 | 必须编写对应的 E2E 测试 |
| Bug 修复 | 先写能复现 Bug 的测试，再修复 |
| 发版前 | 运行全部测试，所有 🔴 项必须通过 |
| 数据依赖 | 测试需处理非交易时段无数据的情况 |

---

## 8. Git 工作流

### 8.1 分支策略

```
main          ← 稳定版本，只接受 PR 合并
├── develop   ← 开发分支（可选，多人协作时使用）
├── feature/* ← 功能分支（feature/multi-market-quotes）
├── fix/*     ← 修复分支（fix/search-input-focus）
└── release/* ← 发版分支（release/v2.3.0）
```

### 8.2 提交信息规范

```
<type>(<scope>): <subject>

类型:
  feat     - 新功能
  fix      - Bug 修复
  refactor - 重构（不影响功能）
  style    - 样式调整
  docs     - 文档更新
  test     - 测试相关
  chore    - 构建/依赖/工具

范围:
  server   - 后端
  frontend - 前端
  desktop  - 桌面壳
  build    - 构建系统
  ci       - CI/CD

示例:
  feat(server): 新增多市场行情自动识别接口
  fix(frontend): 修复搜索高亮状态清除
  refactor(server): 拆分 stockSdk 为子模块
  chore(build): 升级 esbuild 到 0.29
```

### 8.3 版本号

遵循语义化版本 `MAJOR.MINOR.PATCH`：

- **MAJOR**: 不兼容的 API 变更 / 架构重构
- **MINOR**: 新功能，向后兼容
- **PATCH**: Bug 修复，向后兼容

### 8.4 CI/CD

| 触发条件 | 工作流 | 行为 |
|---------|--------|------|
| push / PR 到 main | CI | Node 18/20 构建检查 |
| 推送 `v*` 标签 | Release | macOS + Windows 自动构建 + GitHub Release |
| push 到 main (website/) | Deploy Website | 部署到 GitHub Pages |

**手动发版**：

```bash
git tag v2.3.0
git push origin v2.3.0
```

---

## 9. 构建与部署

### 9.1 构建流程

```
源码
│
├── 前端: Vite build → dist/
│   └── javascript-obfuscator 混淆 → dist/assets/
│
├── 后端: esbuild bundle → server.bundle.js
│   └── javascript-obfuscator 混淆 → 最终产物
│
└── 桌面壳
    ├── macOS: Swift 编译 + 复制产物 → StockToolbox.app
    └── Windows: Electron + NSIS → 工具箱 Setup.exe
```

### 9.2 环境变量

| 变量 | 说明 | 配置位置 |
|------|------|---------|
| `WEB_ROOT` | 静态文件根目录 | Electron 传入 |
| `ELECTRON_USER_DATA` | Electron 数据目录 | Electron 自动设置 |
| `SQLITE_DATA_DIR` | SQLite 数据库路径 | 可选的显式配置 |
| `AI_API_URL` | AI 接口地址 | 应用设置 / 环境变量 |
| `AI_API_KEY` | AI API 密钥 | 应用设置（加密存储） |
| `AI_MODEL` | AI 模型名称 | 应用设置 |

### 9.3 版本检查清单

发版前必须确认：

- [ ] `npm run build:server` 通过（后端 esbuild bundle）
- [ ] `cd react-app && npm run build` 通过（前端 Vite 构建）
- [ ] E2E 测试全部通过
- [ ] macOS `scripts/build-mac.sh` 构建成功
- [ ] Windows `electron:build:win` 构建成功
- [ ] `README.md` 版本号和更新日期已更新
- [ ] 更新日志已撰写

---

## 10. 文档规范

### 10.1 必须维护的文档

| 文档 | 位置 | 更新时机 |
|------|------|---------|
| **README.md** | 项目根目录 | 每次发版 |
| **实施计划** | `实施计划.md` | 新增功能规划时 |
| **开发规范** | `开发规范.md`（本文档） | 规范变更时 |
| **API 文档** | README 中「API 接口」章节 | 新增/修改 API 时 |

### 10.2 文档风格

- 使用中文撰写，技术术语保留英文
- 表格优先于段落，流程图用 ASCII art
- API 文档包含：方法、路径、参数、成功/失败响应示例
- 代码块指定语言（```bash, ```js, ```jsx, ```sql）

---

## 附录 A：技术债务清单

| # | 问题 | 优先级 | 预计工时 | 备注 |
|---|------|--------|---------|------|
| 1 | `server.js` 2700+ 行需拆分 | P1 | 3-4h | 按路由模块拆分 |
| 2 | `stockSdk.js` 977 行需拆分 | P2 | 2-3h | 按数据域拆分 |
| 3 | 构建产物 `server.bundle.js` 在仓库中 | P0 | 0.5h | 添加到 `.gitignore` |
| 4 | 缺少 `.gitignore` 完整配置 | P0 | 0.5h | 覆盖 dist/build/node_modules |
| 5 | CSS `global.css` 缺少模块化 | P3 | 2-3h | CSS Modules 或拆分 |
| 6 | 前端缺少 PropTypes 或 TypeScript | P3 | 8h+ | 可渐进式迁移 |
| 7 | 缺少 API 请求级单元测试 | P2 | 4-6h | 用 Jest 补后端测试 |

---

## 附录 B：工具链版本要求

| 工具 | 最低版本 | 说明 |
|------|---------|------|
| Node.js | ≥ 18 | 使用内置 fetch，无需 node-fetch polyfill |
| npm | ≥ 9 | 包管理 |
| Python | ≥ 3.9 | E2E 测试（Playwright） |
| MySQL | 8.0+ | 可选，无 MySQL 自动回退 SQLite |

---

> **本文档应与项目代码同步更新。发现规范与现状不符时，请同步修正。**
